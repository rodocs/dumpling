+++
Target = "Dragger.MouseDown"
Type = { (mousePart: BasePart, pointOnMousePart: Vector3, parts: BasePart[]): void; (mousePart: BasePart, pointOnMousePart: Vector3, parts: BasePart[]): void; }
+++

Initializes a dragging action, specifying which parts to use when dragging.